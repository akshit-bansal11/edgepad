# Protocol fixtures

Protocol fixtures should include ordinary ASCII and at least one non-ASCII
track title. The latter verifies that length-prefixed or JSON payloads count
encoded bytes correctly instead of relying on the number of Unicode code
points.
